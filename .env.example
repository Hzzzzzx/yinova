# 炼丹 - 环境变量示例
# 复制为 .env 并填入实际值
# 启动面板前: export $(cat .env | xargs) 或 source .env

# OpenClaw 安装目录（含 openclaw 命令）
# 若通过 npm i -g openclaw 安装，可留空；若本地克隆则填路径，如 /Users/xxx/openclaw
OPENCLAW_HOME=

# 智谱 API Key（可选，各卦也可在 openclaw configure 中单独配置）
# ZAI_API_KEY=
